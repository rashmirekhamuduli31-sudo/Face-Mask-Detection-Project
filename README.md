# Face-Mask-Detection-Project
A real-time Face Mask Detection system built with Python, TensorFlow, Keras, and OpenCV. The model uses a Convolutional Neural Network (CNN) to classify whether a person is wearing a mask or not through live webcam detection.
